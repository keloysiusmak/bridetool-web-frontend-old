const mappedStates = [
  'apiToken',
  'accessToken',
  'refreshToken',
  'storedTokensTime',
  'account',
  'activeParty',
  'globalErrors',
  'schedule',
  'parties',
  'budget'
];

const mappedGetters = [
  'tokens'
]

module.exports = {
  mappedStates,
  mappedGetters
}
