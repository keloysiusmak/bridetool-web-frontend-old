const mappedStates = [
  'apiToken',
  'accessToken',
  'refreshToken',
  'storedTokensTime',
  'account',
  'globalErrors'
];

const mappedGetters = [
  'tokens'
]

module.exports = {
  mappedStates,
  mappedGetters
}
